function soma(n1=0, n2=0) { //n1=0 e n2=0 - Os zeros são parâmetros opicionais, quer dizer que se n1 ou n2 não for definido, por padrão, será considerado como 0.
    return n1 + n2
}

console.log(soma(2, 5))